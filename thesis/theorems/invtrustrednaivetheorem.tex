\begin{theorem}[Invariable trust reduction with naive algorithms] \ \\
   \label{invariability}
   If $\forall i \in [n], c_i' \leq x_i$, then $||\delta_i||_1$ is independent of $x_i, c_i'$.
\end{theorem}
